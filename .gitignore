# Nothing generated should ever land in the repo - this site has no build step.
# Only editor / OS leftovers to keep out.

.DS_Store
Thumbs.db
desktop.ini

.idea/
.vscode/
*.swp

# local experiments that must not be published
scratch/
private/
samples/private/
